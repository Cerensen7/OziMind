package com.example.ozimind.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Colors (Boho Palette)
val PrimaryLight = Color(0xFFA3B18A) // Açık Nil Yeşili / Adaçayı (User requested lighter green)
val SecondaryLight = Color(0xFFFCD38C) // Peach Sorbet
val BackgroundLight = Color(0xFFFFFDF8) // Çok hafif sıcak kırık beyaz
val SurfaceLight = Color(0xFFFFFFFF)
val TextColorDark = Color(0xFF5A4A42) // Koyu kahve/boho text rengi
val OnPrimaryLight = TextColorDark // Buton içindeki yazının okunabilir olması için koyu kahve yaptık

// Dark Theme Colors
val PrimaryDark = Color(0xFF8A9A5B)
val SecondaryDark = Color(0xFFFDD0BD) // Soft Blush
val BackgroundDark = Color(0xFF1E1E1E)
val SurfaceDark = Color(0xFF2C2C2C)